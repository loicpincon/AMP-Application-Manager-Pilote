package organisation.application.manager.pilote.commun.controller;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

public class DefaultController {

	protected static final Log LOG = LogFactory.getLog(DefaultController.class);

}
